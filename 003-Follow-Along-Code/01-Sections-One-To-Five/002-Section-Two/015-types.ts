const today: Date = new Date();
today.getMonth(); // if you do today. , it'll list out the methods available to a date object.

const person = {
    age: 20
};

class Color { // class

}

const red = new Color(); // initializing a new instance of the Color type.
