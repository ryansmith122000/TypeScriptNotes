const add = (a: number, b: number): number => {
    return a + b;
};

const subtract = (a: number, b: number): number => {
    return a - b; // returning the type we specify before the =>
}

// This is how you annotate a function properly.
